import { styled } from "@mui/material/styles";

export const BlogCardContianer = styled("div")(() => ({
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  height: "5em",
  width: "7em",
  cursor: "pointer",
}));
export const BlogCardImageHolder = styled("img")(() => ({
  borderRadius: "0.5em",
}));
export const AuthorContainerStyled = styled("div")(() => ({
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  height: "5em",
  width: "7em",
  cursor: "pointer",
}));
export const BlogTitleStyled = styled("img")(() => ({
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  height: "5em",
  width: "7em",
  cursor: "pointer",
}));
