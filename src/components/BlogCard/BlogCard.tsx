import React, { ReactElement, FC } from "react";
import { FooterStyled } from "./BlogCard.styles";

const BlogCard: FC<any> = (): ReactElement => (
  <FooterStyled>Made with 💙 Faker BENZID </FooterStyled>
);

export default BlogCard;
