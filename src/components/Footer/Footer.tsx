import React, { ReactElement, FC } from "react";
import { FooterStyled } from "./Footer.styles";

const Footer: FC<any> = (): ReactElement => (
  <FooterStyled>Made with 💙 </FooterStyled>
);

export default Footer;
